# Required: Anthropic API key for the AI gateway
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional: Auth token shared between proxy and gateway (random generated if omitted)
# GATEWAY_AUTH_TOKEN=

# Optional: Host port mapping (default 18800, avoids conflict with local OpenClaw on 18789)
# CLAWSHIELD_PORT=18800
